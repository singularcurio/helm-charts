apiVersion: v1
kind: Service
metadata:
  name: {{ include "opnsense-mcp.fullname" . }}
  labels:
    {{- include "opnsense-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opnsense-mcp.selectorLabels" . | nindent 4 }}
